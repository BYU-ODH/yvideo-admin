This is where your x509 private key should go it can be generated with the following command:
openssl req -new -x509 -days 3652 -nodes -out sp.crt -keyout sp.key
sp.crt is the certificate that is generated from that command and is in a different file. sp.key is this file and is the required private key
